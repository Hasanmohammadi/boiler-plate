import { Box, styled } from '@mui/material';

const LoginStyledContainer = styled(Box)`
  background: #183a50;
  width: 100%;
  height: 100vh;
  display: flex;
  position: absolute;
  top: 0;
  z-index: 101;
` as typeof Box;

export default LoginStyledContainer;
